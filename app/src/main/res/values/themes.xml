<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ToDoLog" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/deepgreen</item>
        <item name="colorPrimaryVariant">@color/deepgreen</item>
        <item name="colorOnPrimary">@color/offwhite</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/deepgreen</item>
        <item name="colorSecondaryVariant">@color/deepgreen</item>
        <item name="colorOnSecondary">@color/offwhite</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ToDoLog.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ToDoLog.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ToDoLog.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>